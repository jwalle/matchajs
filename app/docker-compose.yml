version: "2"
services:

  web:
    image : node:boron
    volumes:
      - .:/usr/src/app
    working_dir: /usr/src/app
    command: sh -c 'npm install; bower install --allow-root; npm start'
    ports:
        - "3000:3000"
    networks: 
        - all
    tty : true
    
networks:
  all:
